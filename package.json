{
    "scripts": {
        "dev": "vuepress dev docs",
        "build": "vuepress build docs",
        "commit": "vuepress build docs && echo 'blog.yozi.kr' > build/CNAME && git add build -f && git commit -m 'build folder commit'",
        "deploy": "git push origin `git subtree split --prefix build master`:gh-pages --force"
    },
    "devDependencies": {
        "directory-tree": "^2.2.4",
        "vuepress": "^1.2.0"
    },
    "dependencies": {}
}
